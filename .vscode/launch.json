{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run ComSniffer (external console)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/bin/Debug/net8.0/ComSniffer.exe",
            "cwd": "${workspaceFolder}",
            "console": "externalTerminal"
        }
    ]
}